Project /home/leandrosdim777/projects/mySavings, GLM5.2 only. Narrow continuation of prompts/Step03-atomicity-fix-opencode-glm52.md. Preserve auth/user work, applied SQL and .env. No pushes, commits, migrations in public, future steps. Read AGENTS and docs/reviews/Step03-atomicity-correction-opencode.md.

Independent Hermes original real Neon probe now PASSES: tablesSurvivedFailure=0, cleanup0. Preserve this fix. Remaining failed acceptance:
1. node qa-output/step03-independent-scanner-check.cjs: 18/19 pass but legitimate PostgreSQL E-string with escaped quote is rejected: SELECT E'quote\'; COMMIT WORK;'; . Parse E-string escapes correctly (and doubled quoted identifiers), ensure quote/backslash combinations cannot hide actual following transaction control. Reject unterminated single/double/dollar quote and block comment states. Existing scanner accepts SELECT 'unterminated, /* unclosed, DO $$ BEGIN PERFORM 1; . Reject GUC changes to standard_conforming_strings/backslash_quote or similar lexical-mode switches; accepting SET standard_conforming_strings=off invalidates scanner assumptions. Preserve legitimate safe DO/string/comment behavior. Add offline and safe isolated live acceptance/rejection regressions exercising the actual runner. No hostile SQL sandbox claim.
2. env -u DB_TEST_ALLOW_WRITES npx vitest run tests/auth/login.test.ts gives No test files found, NOT requireDatabaseUrl error. CLI filter does not extend include patterns. Fix false comments/report/docs claims. Desired ordinary npm test always offline; explicit test:db needs external permission. Provide supported explicit-file command with correct inclusion and verify missing opt-in refuses before DB. Avoid needless config complexity; dedicated DB config is acceptable. test:watch also must stay offline despite inherited DB_TEST_ALLOW_WRITES=1.

Perform only these corrections, rerun scanner offline/isolated live tests + lint/typecheck/build, document exact real results in same implementer correction report, STOP; no independent approval/registry edits. Do not use pipeline tail exit status as evidence of preceding command success; use pipefail or direct execution. Do not print secrets/raw DB errors. Keep all writes explicit tx and fixtures scoped.
