# Step03 independent review corrections
Project /home/leandrosdim777/projects/mySavings. Use GLM 5.2 Cloud, only Step03. Read AGENTS and current Step03 files. User approved this project's private Neon URL for migrations and isolated disposable tests; never print credentials, preserve .env. No commits/pushes/global edits/auth/business schema/Step04. Keep original self-report and write corrections addendum.

Hermes source review found these concrete gaps; fix and test before stopping:
1. assertTransactionSafe currently rejects VACUUM etc but accepts COMMIT; BEGIN; ROLLBACK; END; START TRANSACTION; PREPARE TRANSACTION; ABORT and migration scripts can escape outer transaction, commit ledger and DDL before later error. Add conservative robust SQL scanning (comments incl nested block comments and quoted/dollar strings handled; DO block BEGIN isn't top-level BEGIN). Reject top-level transaction control and session/search_path overrides that defeat isolation. Test rejected COMMIT/END/ROLLBACK variants, comment-separated controls, and safe SQL with strings/comments/DO. Demonstrate live isolated failure leaves no ledger/schema changes. No general untrusted-SQL sandbox claims.
2. Migration discovery permits missing applied files and newly introduced versions earlier than max applied version. Prevalidate all history and all file checksums before executing pending files: reject missing applied migrations, invalid/duplicate numeric versions, out-of-order history. Deterministic forward-only names, directory/path validation. Do not edit already applied real 0001 SQL. Add tests of real runner on disposable schemas.
3. withTransaction returns clients to pool even if BEGIN/COMMIT/ROLLBACK fails; uncertain/broken connections must be destroyed with release(error)/release(true), preserving primary errors. Add idle pool error handler with safe constant messages. Add mocked tests of real helper to exercise BEGIN failure, callback failure+rollback, COMMIT failure, rollback failure, client release exactly once, pool.connect rejection. Real commit/rollback tests remain. Don't replace integration tests with mocks.
4. Raw error.message/String(error) is logged by db-check and migration CLI. That can leak DB host/user/SQL literals/custom exception secrets. Use sanitized public error codes/config reasons; never raw external messages/details or arbitrary error.name. Test synthetic secret markers in thrown DB errors do not reach logs. App helpers may propagate internal errors for future boundary handling but never log secrets. Document this boundary honestly.
5. pg connectionString ssl options can override explicit ssl:{rejectUnauthorized:true}. Enforce verified TLS after parsing so sslmode=disable/no-verify/uselibpqcompat cannot disable cert/hostname verification; test actual resulting pg Client connectionParameters, not just your config literal. Share connection config where sensible to avoid drift across app/runner/check/test helpers; pin safe verify-full or reject weaker overrides. No printing URL. No TLS relaxation to make tests pass.
6. npm test currently automatically loads real .env and mutates database. Separate default offline unit/finance suite from explicit opt-in integration suite `npm run test:db` requiring e.g. DB_TEST_ALLOW_WRITES=1. Refuse before DB connection without opt-in (no silent skip success for test:db). Unit tests must run with invalid/missing DB env and no network. Integration suite uses uniquely-owned disposable schemas, fixtures and cleanup inside explicit BEGIN/COMMIT/ROLLBACK even for CREATE/DROP SCHEMA. Exact schema prefix+generated identity validation before drop; never broad cleanup. Ensure integration tests use same authorized DB for helper, runner and verification even if optional migration URL exists; don't accidentally create fixtures in one branch and run migrations in another. Runtime migrations accept only a single validated existing schema, no comma-separated search_path or public fallback for disposable-schema tests.
7. Run lint, typecheck, default tests, explicit real test:db with opt-in, build, audit, db:check, migrate twice. Verify cleanup and only public schema_migrations metadata persists. All tests must call real production code paths. Default npm test works offline. Update docs/database.md and self-report with actual findings, counts and limitations. Stop for Hermes independent verification; do not self-mark Step03 verified.
