# Step02 independent review corrections — GLM 5.2 Cloud
Project /home/leandrosdim777/projects/mySavings. Read AGENTS and Step02 scope. Fix only pure financial modules/tests/docs. No DB driver, DB connection, schema, UI, auth, commits or pushes. Real Neon URL in .env: DO NOT read/print/change it or consume it in tests. Current files are your first implementation plus Hermes tests/finance/hermes-review.test.ts. Original suite 82 passed; independent suite 8 failed/2 passed. Do not hide or remove failing requirements.

1. Settlement reversed boolean means an ORIGINAL settlement is reversed and excluded from paid, not a negative unlinked compensating amount. computePaid([{amount:4000,reversed:true}]) must be zero, remaining on 8000 must be 8000. Future DB immutable reversal entries are Step10, not this pure read model. Align original contradictory tests/docs to original business rule, document correction. Validate every historical amount strictly positive safe cents and reversed strictly boolean. Invalid/overpaid histories must reject, not create negative remaining or mark overpayment paid. All exported settlement helpers validate planned nonnegative safe cents and history. Validate constructors. settlePayment invalid input should consistently return ok:false rather than throw unexpectedly; overflow safe; avoid mutating inputs. Define zero-plan status consistently.
2. Forecast validation function currently named validatePositiveCents only checks integer, permitting negative savings targets, negative received/paid, negative expected income. Validate domain nonnegativity for all except balance. Negative transfers reject; from/to IDs valid/distinct. Validate even when setup incomplete.
3. Balance IDs currently add twice. Identical duplicates may be collapsed or rejected; conflicting duplicates MUST reject instead of arbitrarily choosing a number. Same policy for income/expenses/reserves: do NOT choose maximum from contradictory versions. Validate nonblank IDs. If different planned/paid totals coincidentally have equal remainder, still reject conflicting representations.
4. Same liability appearing in ordinary and reserve arrays currently counted in both. Add explicit linkedReserveId for ordinary display if desired, with required existing target and matching representation; reserved ID cannot also count in E. Prove same-ID and different-display-ID linked cases; reject contradictory or missing links rather than guessing. Original test claiming linked reserve only duplicates reserve array and misses cross-category case: add genuine cross-category test.
5. Reserve field outstanding currently treated as gross amount then paid subtracted. Make contract unambiguous: choose planned/protectedTotal plus paid (preferred, update fixtures including Hermes tests for renamed field only), OR true remaining outstanding with no second paid subtraction. Document what caller supplies and partial reserve payment test. Do not leave misleading outstanding-minus-paid API.
6. A balance that exists but is never entered must be representable as amount:null and return setupIncomplete with null forecasts, distinct from amount:0; other entered balances may be aggregated as known partial total but not presented complete. Missing target same semantics. Conflicting duplicate/missing accounts must not be guessed.
7. Test all requested cases including projected/cash-backed effects, negative shortfall, boundary cents against bigint oracle, reversal original exclusion, duplicate IDs/cross-kind linking, malformed historical settlements and nonnegative domain fields. Keep Hermes independent tests; improve catch assertions to expect CentsError on permitted rejection paths, not any exception. Adapt shape only for explicit reserve contract renaming; never weaken financial assertions. Add new tests to cover your fixes; rerun all tests, lint, typecheck, build, audit and diff check.
8. Explain exact numeric parse/format implementation; safe-integer arithmetic acceptable, never decimal float multiplication. Clean needless subtract-zero in formula. Update docs/finance-api.md and self-report with actual initial findings/corrections; do not claim Hermes approval. Do not modify registered future prompts or mark Step03 started. STOP after correction self-report.
