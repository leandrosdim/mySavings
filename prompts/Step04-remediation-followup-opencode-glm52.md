# Finish Step04 remediation — narrow concrete blockers
Project /home/leandrosdim777/projects/mySavings. Exact GLM5.2. Follow AGENTS and prompts/Step04-security-remediation-opencode-glm52.md. All scratch files INSIDE project qa-output (never /tmp or home-level paths: prior runs terminated on external_directory permission errors). Preserve partial implementation/tests, Step03 and existing user work. No push/commit/production/public migrations/Step05/secrets printed.

Current source blockers to FIX, not just document:
1. lib/auth/actions.ts hashes BEFORE admitFailure, and successful guesses bypass admission entirely. Global failure count is under per-email locks only, so rotating-email concurrency races global cap. Replace with actual admission BEFORE any credential lookup/hash: atomic global+per-normalized-account window check and durable reservation, enforced for both success/failure. Bound cross-instance hash concurrency. Private two-user app can use ONE global pg_try_advisory_xact_lock held through the password verification callback; busy requests fail fast with retry message. This bounds concurrency to1 across instances without session locks (Neon pooler safe). Count/reserve under same transaction; execute lookup+real/dummy hash within bounded callback then commit reservation, issue session afterward with existing credential row-lock revalidation. Return value from callback, never framework redirect inside transaction. Expected callback failures must not accidentally roll back attempts. Use DB clock correctly: transaction now() before lock wait is stale; prefer clock_timestamp where required. Avoid exposed expensive dummy hashing before admission. Per-account 5 attempts/5min, global explicit budget, active-window records must never be deleted to bypass quota. Retention cleanup only expired records, bounded batch, automatically invoked under safe tx (no public cleanup during QA). Tests should exercise LOGIN ACTION plus concurrent admission, not copied SQL; ensure blocked requests don't verify a hash. Replace old helper semantics/tests accurately; don't weaken security assertions to fit wrong implementation. No global schema/role changes.
2. Logout route dbError branch currently identical to success 303. Return honest 503/no-store with local Set-Cookie cleared and safe retry/recovery text on failed server revocation. Server action logout must not fake normal success either. Never clear cookies for forbidden Origin.
3. origin.ts accepts any URL by host/protocol, including paths/query/userinfo/fragments and malformed configured origins. Require canonical HTTP(S) origin exactly (optionally normalize one trailing slash for CONFIG ONLY), production HTTPS, reject literal null/missing/multi-origin and credential/path/query/fragment Origin headers. Validate config and return safe503 on bad config; never build redirect from raw invalid origin or blindly trust forwarded host. Correct redirect is new URL('/login',validatedOrigin).
4. Finish failed tests from previous continuation: qa-output/step04-remediation-continuation.log. Existing production session test-only creation bypass must be NODE_ENV=test guarded or remove and use real credential-check path. Guard __setTestPool likewise.
5. Finish genuine production-helper credential reset/login barrier tests both orderings, token revocation, same-device identity tests, logout DB-failure and malformed cookie tests, config/cookie-save failures, direct privileged action exposure, API guard. API guard alone not automatically financial isolation. Document exactly coverage. All DB fixture writes explicit tx; isolated schemas/options ensure production helpers use test schema; never public broad deletes.

Run lint,typecheck,npm test,build,audit,diff check directly or with pipefail. Run DB_TEST_ALLOW_WRITES=1 npm run test:db -- tests/auth after safe fixture review. Continue resolving failures until pass; write docs/reviews/Step04-security-remediation-opencode.md with exact results and remaining risks. No independent review/registry edits. STOP after Step04 remediation, parent does real-browser QA. Do not call /tmp commands or absolute outside-project file tools; if a helper fails avoid another long discovery loop, use current project-local evidence.
