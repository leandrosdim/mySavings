# Step04 bounded security remediation
Project /home/leandrosdim777/projects/mySavings. Use only ollama-cloud/glm-5.2. Implement approved remediation, not Step05 or approval. Read AGENTS.md, docs/product.md, docs/financial-rules.md, steps/Step04.md, docs/auth.md, docs/reviews/Step03.md and Step04.md. Step03 is verified; preserve its runner and test-command safety. Existing raw pg + iron-session + Argon2 email/password choice is approved; no framework replacement or language redesign.

Fix S4-01 through S4-08 and API JSON guard gap. Serialize reset/revoke and session issuance on user row with credential hash revalidation after verification; reset CLI must share real internal reset path and lock order. Remove privileged exports from use-server module (internal server-only/operator CLI only). Atomic DB admission before hashing, per normalized email plus bounded global budget/concurrent hashing across instances, retention, no trusting caller IP headers. Prefer no migration using existing schema/advisory transaction locks if sound. Unknown email must perform valid fixed dummy Argon2 verification at same parameters. All writes including cleanup/tests explicit BEGIN/COMMIT/ROLLBACK one client. Strict configured trusted origin for logout POST (reject missing/null/malformed/sibling/forged forwarded origins), safe non-localhost deployment redirect. Always clear cookie even stale, malformed or DB failure; distinguish revocation failure honestly and document replay limitation during outage rather than false success. Validate cookie config before insertion, handle save failures, remove unnecessary session lookup row locks. API guard must return JSON 401/503 independently of proxy; pages retain redirect.

No commits/pushes, real users, Step05, production/public migrations, global/other-project edits. Never print .env, URLs, raw tokens, passwords, hashes or secrets in output/logs. Do NOT edit applied db/migrations files, docs/reviews/Step03.md, docs/reviews/Step04.md, steps/registry.json, steps/README.md, canonical Step04 prompts. If a migration unavoidable write new identity-only migration, apply ONLY disposable schema. Existing test cleanup currently has nontransactional public fixture writes: refactor auth live suites to disposable isolated schemas and production helper imports, not copied auth SQL. Read cleanup before running. No public/global login_attempt cleanup in tests. Preserve ordinary npm test offline selection even inherited DB_TEST_ALLOW_WRITES=1, and test:db external opt-in.

Add meaningful regressions against production paths: deterministic barrier login/reset both orders; synchronized admission burst incl rotating emails; dummy hash invocation; logout expiry/revoked/tampered/missing and DB revocation error injection with cookie clearing; strict route Origin and deployment redirects; cookie-save failure; JSON guard; token replay/expiry/revocation; scoped A/B and forged owner ignored. Mocks allowed for Next request/cookie boundary but real DB concurrency on isolated schemas required. Maintain existing tests by adapting APIs, never weaken assertions for passing. Document accurate security policy/tradeoffs and safe browser fixture setup/cleanup instructions in docs/auth.md. Do not provision real users or start browsers/servers in this pass.

Run npm run lint, npm run typecheck, npm test, npm run build, npm audit, git diff --check. Live tests allowed on existing project approved test connection ONLY disposable unique step04 schemas; inspect safety first, run DB_TEST_ALLOW_WRITES=1 npm run test:db -- tests/auth, confirm own schemas gone. Do not run db:migrate against public. Report actual counts/failures and migration names at docs/reviews/Step04-security-remediation-opencode.md. This is implementation self-report, not independent approval. Hard stop after remediation and tests; return remaining gaps honestly.