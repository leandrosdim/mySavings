# Step03 bounded atomicity correction
Project /home/leandrosdim777/projects/mySavings. Exact model ollama-cloud/glm-5.2. User approved this correction. Read AGENTS.md, docs/product.md, docs/financial-rules.md, docs/reviews/Step03.md, current db files/tests/package scripts before changing anything. Preserve all existing uncommitted work including independently user-implemented Step04. No auth changes, new migrations, Step05 work, commits/pushes, global config changes, production migrations or real-user changes. Do not print .env, connection strings, credentials or raw DB errors. Raw pg only; explicit same-client write transactions.

Fix production migration scanner transaction escape: COMMIT WORK, COMMIT TRANSACTION, COMMIT AND CHAIN/AND NO CHAIN, ROLLBACK variants, END WORK, ABORT variants, START/**/TRANSACTION and commented keyword forms. Reject transaction-control statement families regardless of clauses and valid whitespace/comments, including PREPARE TRANSACTION. Audit session/search_path/role overrides, quoted setting identifiers, SET LOCAL and RESET variants. Preserve legitimate strings, nested comments, dollar quotes and DO block internal BEGIN/END. Handle escape strings safely; reject malformed/ambiguous SQL rather than allowing guard bypass. Do not claim this is a sandbox for malicious procedural SQL: document trusted reviewed migrations boundary.

Use docs/reviews/Step03.md real failing sequence CREATE TABLE escaped(id integer); COMMIT WORK; SELECT 1/0; as regression. Add offline production-scanner tests and isolated live production-runMigrations tests that prove no table/ledger persists, plus retained valid DO and history/concurrency regression cases. Only unique disposable schemas against already approved test connection; cleanup exact fixtures and verify counts. Never run failure probes in public schema. Do not alter applied migration SQL or ledger.

Fix test safety: ordinary npm test stays offline regardless of inherited DB_TEST_ALLOW_WRITES; test:db must require explicit external DB_TEST_ALLOW_WRITES=1, not set its own permission. Ensure tests are not silently excluded when explicitly selected. Document exact offline/live commands. Preserve shared auth tests and don't execute unsafe broad cleanup. Inspect test fixture scoping before DB test execution.

Run lint/typecheck/offline tests/build/audit/diff check and targeted opt-in migration tests; record real results and any blockers in docs/reviews/Step03-atomicity-correction-opencode.md. Leave independent review and registry approval to Hermes. Hard stop after this bounded Step03 correction.
