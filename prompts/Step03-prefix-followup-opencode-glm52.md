NARROW final Step03 lexical bug, /home/leandrosdim777/projects/mySavings, GLM5.2 only. Preserve all unrelated work and current fixes. No public migrations, auth edits, push/commit/env printing. Read AGENTS. Do not redo broad discovery.
Hermes found your new E-string lexer falsely treats E followed by whitespace then quote as E-string and recognizes trailing e inside identifiers. PostgreSQL E prefix MUST be adjacent to quote and a standalone lexical prefix, not final letter of name/CASE. This creates a new transaction escape. Actual assertTransactionSafe accepts this exact valid PostgreSQL sequence (backslash below is a literal single backslash):
```sql
SELECT name '\'; COMMIT WORK; -- '
SELECT 1;
```
The scanner merges the whole sequence as one SELECT. Reject actual COMMIT WORK here. Remove incorrect whitespace E prefix branch and require proper lexical boundary for contiguous E/e' prefix (preceding identifier character must prevent recognition; account Unicode identifier chars conservatively). Add regression for typed name literal and genuine E escape strings followed by COMMIT, safe E strings, CASE/name strings, and ASCII CR comments if relevant. Use real runner isolated schema test with CREATE TABLE escaped(id integer); followed by this sequence and a final SELECT 1/0 to prove no table/ledger remains. Keep guard trusted-migrations-only and existing malformed-state checks. Run offline scanner, targeted live migration tests using current dedicated DB config, independent qa-output/step03-independent-scanner-check.cjs, lint/typecheck. Append actual results to implementer report, hard STOP. Never edit applied SQL or Hermes independent reports/registry.
