Project /home/leandrosdim777/projects/mySavings. Narrow final Step04 corrections, GLM5.2 only, no commits/pushes/Step05/public fixture writes/global edits/secrets. Read AGENTS, existing implementation. DO NOT redo entire review or broad discovery. Previous self-report incorrectly claims tests and logoutAction behaviors that do not exist. Fix:
1 actions.ts logoutAction dbError and success BOTH redirect /login; return/throw an honest safe failure on dbError with cookie clearing. session.clearSessionCookie swallows errors while dal.logout always reports cookieCleared true; don't assert false success. Route must preserve explicit clearing header and503; test real route POST, not just helper. Test403 no clearing/mutation,303 deployment redirect+clearing/no-store,503+clearing/no-store on actual revocation failure after successful lookup. Distinguish lookup failure. Use mocks at Next boundary, isolate DB.
2 session.ts insertion commits then getIronSessionData OUTSIDE cleanup try. Cover initialization and save failures with cleanup, config fails before insert; ensure safe sanitized cleanup-failure behavior.
3 rate-limit cleanupOldLoginAttempts has no caller. Integrate bounded expired-record-only cleanup inside admission transaction (same client, no nested locks), preserve active budget, cleanup tested. Document accepted strict private-app availability policy: global100 failures/10min and peraccount5/5min can temporarily block legitimate login; not 'no account lockout'. Hash concurrency1 failfast deliberate.
4 tests/auth/barrier.test.ts reset tests sequential despite name. Add actual deterministic barriers using separate clients/pool instrumentation and production helper calls: pause issuance after user lock before commit, start reset confirm waits then release => session revoked; pause reset after user lock start stale issuance confirm waits then release => credential mismatch/no new session. Avoid time-based sleep-only evidence; assert pg lock waiting or synchronized query instrumentation while real transactions overlap.
5 global-budget test currently3 emails and checks constant positive. Fill isolated fixture to global limit minus1 (explicit fixture tx), invoke two DIFFERENT emails, assert only1 new callback admitted and no overshoot then blocked callbacks ZERO hashing. Also hold admission callback open with deferred barrier, competitors distinct accounts must busy and zero callbacks, max concurrent1; loginAction-level spy verifies blocked request doesn't call verifyPassword. Real production helpers, not copied SQL.
6 logout-guard.test never imports POST despite claim. Add direct route tests as above and action failure test. Preserve existing assertions, fix stale claims docs/report. All fixture writes explicit transactions, disposable schemas only. No cross-project/tmp file tools.
Run lint/typecheck/npm test/build/audit/diff-check and opt-in live auth tests. Report real output in docs/reviews/Step04-final-regressions-opencode.md. Do not edit independent review or registry. Stop after these bounded fixes and tests; parent does browser QA. Prefer implement now, not repeated long discovery/todo narration.