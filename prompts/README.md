# OpenCode prompt register

All Step01–Step18 execution prompts are prepared. See [the ordered register](../steps/README.md) for scope, prerequisites and verified status. Canonical step files and their execution copies must remain byte-identical when amended.

Use only `ollama-cloud/glm-5.2`, one approved prompt at a time. Step01 is complete; its registered prompt defaults to verification-only. `archive/Step01-original-executed-opencode-glm52.md` preserves the actual original implementation prompt and must not be re-executed blindly.

Prepared is not approved, implemented or verified. Future prompts explicitly stop on unresolved credentials/decisions. Refresh code/schema assumptions at launch; never run the whole directory automatically.
